{
  "name": "churn",
  "version": "0.1.0",
  "description": "Time-decay-based task management with priority curves",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "churn": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["task", "todo", "priority", "time-management", "cli"],
  "author": "Kevin",
  "license": "MIT",
  "dependencies": {
    "@kevjava/task-parser": "file:../task-parser",
    "better-sqlite3": "^11.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist/**/*",
    "README.md"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}
